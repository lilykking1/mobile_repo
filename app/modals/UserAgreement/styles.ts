import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  agreementContainer: {
    marginHorizontal: 24,
  },
  container: {
    height: '100%',
  },
  iconLeft: {
    left: 16,
    position: 'absolute',
    top: 52,
  },
  title: {
    marginLeft: 24,
    marginTop: 105,
  },
});

export default styles;
