import { StyleSheet } from 'react-native';
import { NEWS_LIST_HEADER_HEIGHT } from '../../constants';

const styles = StyleSheet.create({
  newsListHeader: {
    height: NEWS_LIST_HEADER_HEIGHT,
    justifyContent: 'flex-start',
  },
});

export default styles;
