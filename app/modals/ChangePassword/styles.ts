import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerContent: {
    paddingBottom: 14,
  },
  containerModal: {
    marginBottom: 14,
    paddingBottom: 50,
  },
  textInput: {
    width: '100%',
  },
});

export default styles;
