import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  coin: {
    left: 125,
    position: 'absolute',
  },
  title: {
    flexDirection: 'row',
    width: '100%',
  },
});

export default styles;
