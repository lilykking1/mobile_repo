import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonSeparator: {
    marginBottom: 12,
  },
  modalContent: {
    width: '75%',
  },
  subTitleEdit: {
    lineHeight: 20,
    marginBottom: 24,
    marginTop: 8,
    textAlign: 'center',
  },
  titleEdit: {
    lineHeight: 24,
    textAlign: 'center',
  },
});

export default styles;
