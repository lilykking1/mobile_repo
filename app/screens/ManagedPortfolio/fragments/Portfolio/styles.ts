import { StyleSheet } from 'react-native';

import { palette } from '@app/theme';

const styles = StyleSheet.create({
  margin: {
    marginBottom: 16,
  },
  riskAppetiteContainer: {
    backgroundColor: palette.white,
    borderRadius: 20,
    marginBottom: 80,
    marginTop: 14,
    width: '100%',
  },
});

export default styles;
