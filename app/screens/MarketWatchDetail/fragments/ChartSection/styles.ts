import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerValue: {
    alignItems: 'center',
    marginBottom: 30,
  },
  text: {
    marginBottom: 4,
  },
});

export default styles;
