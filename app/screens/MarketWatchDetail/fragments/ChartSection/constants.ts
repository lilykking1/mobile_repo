import { palette } from '@app/theme';
import { ColorsVariant } from './types';

export const GRADIENT_COLORS: ColorsVariant = {
  light: palette.grey[300],
  dark: palette.royalBlue[1000],
};

export const LINE_COLORS: ColorsVariant = {
  light: palette.primary,
  dark: palette.royalBlue[400],
};
