import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 8,
  },
  subtitle: {
    marginBottom: 16,
    marginTop: 8,
    textAlign: 'center',
  },
  title: {
    textAlign: 'center',
  },
});

export default styles;
