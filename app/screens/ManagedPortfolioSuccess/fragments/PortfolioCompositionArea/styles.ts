import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cards: {
    minHeight: 88,
  },
  margin: {
    marginBottom: 16,
  },
  portolioComponsitionTitle: {
    marginBottom: 24,
  },
});

export default styles;
