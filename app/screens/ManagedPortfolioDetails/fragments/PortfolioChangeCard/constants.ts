import { palette } from '@app/theme';

export const lightTheme = palette.grey[500];
export const DarkTheme = palette.grey[700];

export const underlineColorThemes = {
  light: lightTheme,
  dark: DarkTheme,
};
