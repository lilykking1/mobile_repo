import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonSeparator: {
    marginBottom: 12,
  },
  subTitleEdit: {
    marginBottom: 20,
    marginTop: 8,
    textAlign: 'center',
  },
  titleEdit: {
    lineHeight: 24,
    textAlign: 'center',
  },
});

export default styles;
