import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actionsContainer: {
    alignItems: 'center',
  },
  discloser: {
    marginBottom: 16,
    textAlign: 'center',
  },
  logoutButton: {
    marginBottom: 10,
    marginTop: 5,
  },
});

export default styles;
