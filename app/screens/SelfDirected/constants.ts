import { palette } from '@app/theme';

export const GRADIENT_COLOR = {
  light: palette.grey[300],
  dark: palette.royalBlue[1000],
};

export const ICON_COLOR = {
  light: palette.royalBlue[1000],
  dark: palette.grey[300],
};
