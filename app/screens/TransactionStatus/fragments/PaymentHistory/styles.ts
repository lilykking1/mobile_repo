import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    marginBottom: 12,
    marginTop: 10,
  },
  transactions: {
    paddingBottom: 40,
  },
});

export default styles;
