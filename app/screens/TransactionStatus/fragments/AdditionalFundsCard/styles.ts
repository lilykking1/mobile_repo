import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 24,
    marginHorizontal: 16,
    marginTop: 30,
    padding: 32,
  },
  title: {
    marginBottom: 20,
  },
});

export default styles;
