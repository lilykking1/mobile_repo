import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  hide: {
    display: 'none',
  },
  show: {
    display: 'flex',
  },
  view: {
    flex: 1,
  },
});

export default styles;
