import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
  },
  logo: {
    alignSelf: 'center',
    height: 48,
    marginTop: 25,
    width: 167,
  },
});

export default styles;
