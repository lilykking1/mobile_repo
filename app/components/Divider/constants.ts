import { palette } from '@app/theme';

const lightBackgroundColor = palette.grey[400];
const darkBackgroundColor = palette.grey[700];

export const COLORS = {
  light: lightBackgroundColor,
  dark: darkBackgroundColor,
};
