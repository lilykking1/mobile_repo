import { ToggleIconsVariant } from '../components/ToggleIcon/types';

const variants: Record<ToggleIconsVariant, ToggleIconsVariant> = {
  primary: 'primary',
  success: 'success',
  danger: 'danger',
  warning: 'warning',
  default: 'default',
};

export default variants;
