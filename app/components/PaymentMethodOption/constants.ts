import { palette } from '@app/theme';
import { ThemeVariant } from './types';

export const ICON_TINT_COLOR: ThemeVariant = {
  light: palette.royalBlue[500],
  dark: palette.royalBlue[400],
};

export const DEFAULT_OPACITY = 0.7;
