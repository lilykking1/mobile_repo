import { paletteColors } from '@app/theme';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderColor: paletteColors.green[500],
    justifyContent: 'center',
  },
});

export default styles;
