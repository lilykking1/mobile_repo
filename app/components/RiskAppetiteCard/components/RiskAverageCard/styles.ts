import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerText: {
    borderRadius: 4,
  },
  percentage: {
    textAlign: 'center',
    width: '100%',
  },
  riskAvgCard: {
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 4,
  },
  riskAvgCardContainer: {
    alignItems: 'center',
    borderRadius: 10,
    height: 64,
    justifyContent: 'center',
    width: 80,
  },
  subtitle: {
    marginTop: 10,
  },
});

export default styles;
