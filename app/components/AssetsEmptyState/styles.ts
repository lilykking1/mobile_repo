import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardContainer: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
  label: {
    alignSelf: 'center',
    justifyContent: 'center',
    marginBottom: 25,
    textAlign: 'center',
  },
  storyBookDoubleButtonView: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    width: '100%',
  },
});

export default styles;
