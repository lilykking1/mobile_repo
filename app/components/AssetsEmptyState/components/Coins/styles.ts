import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  blueCoin: {
    bottom: 110,
    position: 'absolute',
    right: 0,
  },
  goldCoin: {
    left: 0,
    position: 'absolute',
    top: 47,
  },
});

export default styles;
