import { LabelAlignmentVariants } from '../types';

export const labelAlignmentVariants: Record<
  LabelAlignmentVariants,
  LabelAlignmentVariants
> = {
  right: 'right',
  left: 'left',
  top: 'top',
  bottom: 'bottom',
};
