import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    alignSelf: 'center',
    marginBottom: 10,
    width: 200,
  },
  header: {
    marginBottom: 12,
    textAlign: 'center',
  },
  modal: {
    marginBottom: 100,
  },
});

export default styles;
