import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  background: {
    borderRadius: 4,
    paddingHorizontal: 4,
  },
  container: {
    left: 8,
    marginLeft: 12,
    position: 'absolute',
    top: -10,
    zIndex: 2,
  },
  hint: {
    lineHeight: 20,
  },
});

export default styles;
