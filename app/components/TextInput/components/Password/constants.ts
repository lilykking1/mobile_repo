import { palette } from '@app/theme';

export const INPUT_COLOR_VARIANT = {
  light: palette.royalBlue[900],
  dark: palette.white,
};

export const INPUT_PLACEHOLDER_VARIANT = {
  light: palette.grey[650],
  dark: palette.grey[650],
};
