import { TextButtonSize, TextButtonVariant } from '../types';

export const sizes: Record<TextButtonSize, TextButtonSize> = {
  normal: 'normal',
  small: 'small',
  large: 'large',
};

export const variants: Record<TextButtonVariant, TextButtonVariant> = {
  primary: 'primary',
  secondary: 'secondary',
};
