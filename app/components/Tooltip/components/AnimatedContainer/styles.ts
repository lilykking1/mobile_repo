import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tooltipContainer: {
    alignItems: 'center',
    borderRadius: 20,
    paddingHorizontal: 8,
    paddingVertical: 4,
    position: 'absolute',
  },
});

export default styles;
