import { palette } from '@app/theme';

export const lightThemeSeparator = palette.grey[400];
export const darkThemeSeparator = palette.grey[700];

export const separatorStyleTheme = {
  light: lightThemeSeparator,
  dark: darkThemeSeparator,
};
