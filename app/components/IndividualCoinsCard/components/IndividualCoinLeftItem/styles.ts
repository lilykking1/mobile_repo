import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  coinIconContainer: {
    marginRight: 16,
  },
  coinLabel: {
    marginBottom: 4,
  },
  leftItemContainer: {
    flex: 4,
    flexDirection: 'row',
  },
});

export default styles;
