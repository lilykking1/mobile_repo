import { RadioButtonSize, RadioButtonVariant } from '../types';

export const sizes: Record<RadioButtonSize, RadioButtonSize> = {
  normal: 'normal',
  small: 'small',
};

export const variants: Record<RadioButtonVariant, RadioButtonVariant> = {
  primary: 'primary',
  success: 'success',
  danger: 'danger',
  warning: 'warning',
  transparent: 'transparent',
};
