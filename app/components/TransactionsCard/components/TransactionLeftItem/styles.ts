import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  addressContainer: {
    flexDirection: 'row',
  },
  coinIconContainer: {
    marginRight: 16,
  },
  fromText: {
    marginRight: 8,
  },
  leftTxContainer: {
    flexDirection: 'row',
  },
});

export default styles;
