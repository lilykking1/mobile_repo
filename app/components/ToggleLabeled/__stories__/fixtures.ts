import { ToggleVariant } from '../components/ToggleLabeled/types';

const variants: Record<ToggleVariant, ToggleVariant> = {
  primary: 'primary',
  success: 'success',
  danger: 'danger',
  warning: 'warning',
  default: 'default',
};

export default variants;
