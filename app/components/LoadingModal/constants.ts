import { ModalVariants, BlurType } from './types';

export const MODAL_TRANSPARENCY = {
  light: ModalVariants.WHITE,
  dark: ModalVariants.BLACK,
};

export const MODAL_BLUR_TYPE = {
  light: BlurType.light,
  dark: BlurType.dark,
};
