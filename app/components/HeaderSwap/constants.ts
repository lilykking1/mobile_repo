import { palette } from '@app/theme';

export const SLIDER_STEP = 0.0000001;

export const ICON_THEME_VARIANTS = {
  light: palette.royalBlue[900],
  dark: palette.white,
};

export const STEP_NEED_ANIMATE_HEADER = [false, false, true];

export const DEFAULT_PRECISION = 7;
