import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 9,
    borderWidth: 1,
    paddingHorizontal: 9,
  },
  text: {
    lineHeight: 28,
  },
});

export default styles;
