import React, { FC } from 'react';
import { View } from 'react-native';

import styles from './styles';

const DotNotConnected: FC = () => <View style={styles.container} />;

export default DotNotConnected;
