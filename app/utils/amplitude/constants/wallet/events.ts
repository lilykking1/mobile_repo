export const enum AmplitudeWalletEvents {
  CLICK_SWAP_SELECT_TYPE = 'swap select type',
  CLICK_RETRY_SWAP_ERROR = 'swap error click retry swap',
  WITHDRAW = 'click withdraw from wallet',
  DEPOSIT = 'click deposit on wallet',
  SWAP = 'click on swap on wallet',
  BUY_CRYPTO = 'click buy crypto on wallet',
}
